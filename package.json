{
  "name": "jenkins-build-trigger-action",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "axios": "^0.24.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.32.0"
  },
  "scripts": {
    "build": "ncc build index.js -m -o dist"
  }
}
